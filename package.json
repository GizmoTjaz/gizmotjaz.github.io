{
  "name": "gizmotjaz.github.io",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --cleanup"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.13.1",
    "core-js": "^3.8.3",
    "nuxt": "^2.15.4"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
